%h1 Listing users

%table
  %thead
    %tr
      %th Email
      %th First name
      %th Last name

  %tbody
    - @users.each do |user|
      %tr
        %td= user.email
        %td= user.first_name
        %td= user.last_name
